# DISCOVA

Website Visibility Intelligence Engine — powered by lean.X digital.

- `BUILD-PROMPT.md` — the build specification. Start there.
- `SEO_AUDIT_FRAMEWORK.md` — the audit methodology (v2.2).
- `web/` — Next.js interface (deploys to Vercel).
- `worker/` — analysis worker (deploys to Railway).
- `supabase/migrations/` — database schema, applied in order.
- `templates/`, `assets/` — report design sources.
